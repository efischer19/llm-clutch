name: Build Docker Images

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # This workflow is preserved for future Docker builds.
  # Currently, no applications are containerized, but the infrastructure
  # remains for when Docker images are needed again.
  placeholder:
    name: Docker Build Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
      - name: Docker build infrastructure ready
        run: echo "Docker build infrastructure is available when needed"
